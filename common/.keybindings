# Keybindings for tmux-sessionizer: bindkey in zsh, bind in bash.
if [[ -n "$ZSH_VERSION" ]]; then
    bindkey -s "^f" '$HOME/.local/scripts/tmux-sessionizer\n'
else
    bind '"\C-f":"tmux-sessionizer\n"'
fi
